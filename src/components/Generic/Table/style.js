import styled, { css } from 'styled-components';

const center = css`
  display: flex;
  align-items: center;
`;
export const Wrap = styled.table``;

Wrap.Tbody = styled.tbody``;
Wrap.Thead = styled.thead``;
Wrap.Tr = styled.tr``;
Wrap.Td = styled.td``;
